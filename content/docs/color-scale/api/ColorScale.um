@prototype hx.ColorScale
  @constructor
    @description
      A class for converting a number to a color, using a mapping from a linear
      scale to an array of colors. Interpolation happens between colors in the
      color array.

    @inline ColorScale/arguments/start.um
    @inline ColorScale/arguments/end.um
    @inline ColorScale/arguments/range.um

  @inline ColorScale/methods/*.um
