@version 1.3.1
  @api Palette
    @bugfix: Fixed an issue where calline @code(js)[hx.palette.context(selection, undefined)] would return @code(js)[undefined] instead of the selection. This was introduced accidentally as part of 1.3.0.