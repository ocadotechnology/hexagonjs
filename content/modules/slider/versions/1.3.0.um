@version 1.3.0
  @api Slider
    @bugfix: Update so the change event is only emitted when the value is actually changed
      @issue 183
    @bugfix: Update so the slidestart event is only emitted on slidestart, not on change
      @issue 184