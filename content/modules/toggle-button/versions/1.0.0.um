@version 1.0.0
  @api Toggle Button
    @function hx.toggleButton
      @added: Fluid api version of ToggleButton
      @description: Creates a new ToggleButton set up on a detached element, wrapped in a selection
      @param options [Object]: See the options object for constructing ToggleButton
      @returns Selection: A selection containing an element with an ToggleButton initialised on it