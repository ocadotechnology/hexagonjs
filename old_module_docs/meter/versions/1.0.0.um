@version 1.0.0
  @api Meter
    @function hx.meter
      @added: Fluid api version of Meter
      @description: Creates a new Meter set up on a detached element, wrapped in a selection
      @arg options [Object]: See the options object for constructing Meter
      @returns Selection: A selection containing an element with an Meter initialised on it
